# 🔰 GoogleTranslateBot

🛠The following technologies were used in this project: Python, aiogram, sqlite3, Admin Panel✅

📝 Brief information:
🤖This bot can translate from Uzbek to 12 languages, or vice versa, from 12 languages ​​to 🇺🇿 Uzbek!

## Setup

- run `git clone https://github.com/kenjayevdev/GoogleTranslateBot.git` copy repositories
- run `cd GoogleTranslateBot` accessing repositories
- run `python -m venv env` to create virtual environment
- run `env\Scripts\activate` to activate the env
- run `pip install -r requirements.txt` to install all required packages
- Create a .env file and set ADMINS=#Telegram user ID and BOT_TOKEN=#BOT API KEY.
- run `python app.py`
